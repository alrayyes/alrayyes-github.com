---
pageable: true
per_page: 10
---

<% page_articles.each_with_index do |article, i| %>
  <%= partial "article_list_element", :locals => {:article => article} %>
<% end %>

<% if paginate %>
  <div class="pagination pagination-centered">
    <%= pagination_links %>
  </div>
<% end %>

